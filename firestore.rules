rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // URLを知っている人全員が読み書き可能（認証なし共有モデル）
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
